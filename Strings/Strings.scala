object Strings{
	def main(args: Array[String]){
		val message : String = "Hey people!"
		var m1= "Hakuna";
		var m2="Matata";
		var len = message.length();
		println(message)
		println("length of the message is"+ len);
		println("The new message is"+m1+m2);
	}
}
